[pytest]
testpaths = tests
# The project has no package layout; modules are imported as tools.* and
# stuff.* relative to the repository root.
pythonpath = .
filterwarnings =
    # Pre-existing invalid escape sequences in regex literals across stuff/.
    # Not this branch's scope; tracked separately.
    ignore::SyntaxWarning
